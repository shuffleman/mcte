# daili3.pcap MCTE 伪装流量检测报告

> 模型: XGBoost | 训练数据: Vanilla + LuckyBlock + Pixelmon (真实MC) vs daili2 (MCTE)
> 测试数据: daili3.pcap | 窗口: 300 | 特征: 56

## 1. 分类结果

| 判定 | 窗口数 | 占比 |
|---|---|---|
| **MCTE 伪装流量** | **297** | **99.0%** |
| Real MC | 3 | 1.0% |

| 置信度 | 窗口数 |
|---|---|
| 高置信 MCTE (prob > 0.9) | 257 |
| 低置信 MCTE (prob 0.5–0.9) | 40 |
| 模糊 (0.3–0.5) | 2 |
| 低置信 Real (0.1–0.3) | 1 |
| 高置信 Real (< 0.1) | 0 |

- **平均 MCTE 概率:** 0.92
- **结论:** daili3 被模型强烈判定为 MCTE 伪装流量

## 2. 判定依据 (SHAP 全局特征影响力)

| 特征 | SHAP 影响力 | 维度 | 物理含义 |
|---|---|---|---|
| `seq_delta_rate` | 3.51 | TCP 行为 | SEQ 序列号变化速率 |
| `c2s_small_pkt_frac` | 1.41 | 包大小分布 | C→S 小包占比 |
| `iat_max_ms` | 1.19 | 到达间隔 | 最大包到达间隔 |
| `pl_hfb_s2c_top5_ratio` | 0.33 | 载荷熵 | S→C 高频字节 Top5 占比 |
| `pl_ent_s2c_mean` | 0.30 | 载荷熵 | S→C 载荷熵均值 |
| `c2s_payload_pkt_frac` | 0.20 | 流量统计 | C→S 有效载荷包占比 |
| `pl_ent_c2s_mean` | 0.21 | 载荷熵 | C→S 载荷熵均值 |
| `c2s_size_mean` | 1.49 | 包大小分布 | C→S 包大小均值 |
| `c2s_size_std` | 0.12 | 包大小分布 | C→S 包大小标准差 |
| `iat_median_ms` | 0.11 | 到达间隔 | 中位包到达间隔 |

## 3. 核心特征偏离分析

### 3.1 TCP 序列号速率 (`seq_delta_rate`) — 影响力 3.51

最关键的判定依据，影响力是第二名的 2.5 倍。

| 指标 | 真实 MC | daili3 (MCTE) | 差距 |
|---|---|---|---|
| SEQ delta rate | ~5 亿/s | ~40 亿/s | **8 倍** |

**原因:** MCTE 隧道在两端各自独立维护 TCP 连接，内部承载的加密流量导致序列号递进速率远超原生 Minecraft 的单连接行为。这是操作系统 TCP 栈层面的特征，加密无法掩盖。

### 3.2 C→S 小包比例 (`c2s_small_pkt_frac`) — 影响力 1.41

| 指标 | 真实 MC | daili3 (MCTE) |
|---|---|---|
| 小包比例 (≤100B) | ~1.0 (近 100%) | 显著偏离 |

**原因:** 真实 MC 客户端到服务端方向几乎全是 TCP ACK + keep-alive 小包 (44 字节)。MCTE 隧道将此方向的载荷加密后撑大，小包比例下降。

### 3.3 最大包到达间隔 (`iat_max_ms`) — 影响力 1.19

| 指标 | 真实 MC | daili3 (MCTE) |
|---|---|---|
| 最大 IAT | ~50ms | ~545ms (10 倍) |

**原因:** MC 客户端与服务端同线程连续发包，间隔极短 (<1ms)。MCTE 隧道引入代理转发延迟和加密开销，最大间隔显著扩大。

### 3.4 载荷熵特征

| 特征 | 真实 MC | daili3 (MCTE) |
|---|---|---|
| S→C 载荷熵均值 | ~0.7 | 6.15 |
| S→C 高频字节 Top5 占比 | ~0.3 | 0.15 |

**原因:** MC 明文协议有固定 packet ID 和字段结构，字节频率分布不均 (低频熵)。AES 加密后载荷接近均匀随机分布 (高熵)，高频字节集中度下降。

## 4. 总结

模型从三个维度判定 daili3 为 MCTE 伪装流量：

1. **TCP 层 (最关键):** SEQ 序列号速率异常偏高，代理双连接架构的固有特征
2. **时序层:** 包到达间隔被隧道延迟撑大
3. **载荷层:** 加密使熵值升高，字节频率分布偏离 MC 明文特征

这三类特征均属于**物理层/传输层特性**，即使 MCTE 隧道对载荷做填充和伪装，也无法消除 TCP 行为层面的指纹。
